import React from "react"
import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  tituloImage: {
    paddingTop: 10,
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#8D4600'
  }
})